win:
  target:
    - target: nsis
      arch: x64

  icon: build/icon.ico

  publish:
    - provider: github
      protocol: ssh

mac:
  target:
    - target: dmg
      arch: arm64

    - target: pkg
      arch: x64

  icon: build/icon.icns

  publish:
    - provider: github
      protocol: ssh